const router = require("express").Router({ mergeParams: true });
/*############# WORKDETAILS DATABASE API CALLS #####################*/
//Add WorkDetails for a specific User
router.post("/workDetails", (req, res, next) => {});

module.exports = router;
